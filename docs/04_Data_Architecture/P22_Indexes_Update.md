# П22. Актуализация модели и индексы

**Индексы:**
- Индекс по номеру заказа в `orders` для быстрого поиска.
- Индекс по `pvz_id` в `orders` и `load_logs`.
- Индекс по `created_at` для аналитики по периодам.
