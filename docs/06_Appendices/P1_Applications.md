# Приложения

## Приложение А

### Схема базы данных (ERD)

[Смотреть актуальную ERD-схему базы данных](../04_Data_Architecture/P26_DB_Schema.md)

Схема отражает текущую структуру БД проекта, построенную по миграциям в `backend/migrations`, включая таблицы `users`, `pvz`, `operations`, `refresh_tokens`, `notifications`, `products`, `marketplace_orders` и связанные справочники.

## Приложение Б

### Спецификация REST API (Swagger UI)

[Здесь вставляется скриншот интерфейса Swagger UI спецификации]

Скриншот интерфейса Swagger UI, демонстрирующий список всех доступных эндпоинтов `/api/auth`, `/api/orders`, `/api/cells`, `/api/actions`, сгруппированных по тегам, а также примеры структуры моделей данных `Order`, `Cell`, `User` и `Action`.
